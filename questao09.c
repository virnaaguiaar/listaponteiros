p = mat + 1;
        // novo endereço que aponta para o próximo lementor do vetor 'mat'

p = mat;
        // endereço que aponta para o 1° elemento do vetor 'mat'

x = (*mat);
        // valor do 1° elemento do vetor 'mat'
